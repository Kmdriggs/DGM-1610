using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEmgine.UI;

public class outdated : MonoBehaviour
{
    public Text myText; //use this instead of GUItext 
    public Image myTexture; //use instead of GUITexture


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
